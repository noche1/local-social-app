# 우리 고장 공공기관 지도 탐험 — 진행 현황

## 프로젝트 개요
- **대상**: 초등학교 3학년, 사회과 [우리 생활에 도움을 주는 장소] 단원 연계
- **목적**: 우리 지역(고장) 안의 공공기관(우체국, 소방서, 경찰서, 병원 등)을 실제 지도에서 찾아 클릭하며 배우는 확인 학습 활동
- **용도**: 디지털연구대회 출품용 웹앱 (2026 충남 디지털 연구대회)
- **진행 방식**: 교실 안에서 진행, GPS 체크인으로 미션을 강제하지 않음 (클릭 확인 기반)
- **개인화**: 시작 화면에서 '우리 학교'를 검색해 지정하면, 카테고리마다 학교에서 가장 가까운 시설이 지도에 표시됨. 특정 학교를 코드에 박지 않아 **충남 어느 학교든 그대로 사용 가능** (대회 범용성)
- **데이터 저장**: 미션 진행은 React state만 사용 — 새로고침하면 초기화(한 차시용). '우리 학교' 설정만 localStorage에 유지(교사가 한 번 지정). 서버/교사 대시보드 없음

## 기술 스택
- React + Vite
- react-router-dom (페이지 라우팅)
- 카카오맵 JavaScript SDK (동적 로드, `VITE_KAKAO_MAP_KEY` 환경변수 사용)

## 화면 흐름
```
Home (/)
  ├─ "우리 학교 찾아볼까요?"       학교 이름 검색(카카오 장소 검색, 충남만) → 선택하면
  │                               경계 데이터로 소속 시군 자동 판별 → localStorage 저장
  │   ├─ "📍 내 위치로 바로 찾기"   브라우저 GPS로 현재 좌표를 그대로 기준점으로 저장
  │   │                           (데스크톱은 Wi-Fi/IP 기반이라 부정확할 수 있음 — 학교
  │   │                            검색의 보조 옵션이지 대체 아님)
  │   └─                          → "우리 고장 탐험 가기" / "다시 정하기"
  └─ "시군 지도 보러 가기"
       ↓
RegionSelect (/regions)          충남 15개 시군을 지도에 표시(시군 경계선 포함), 클릭해서 이동
  ├─ 우리 학교가 있는 시군은 ⭐ 표시
  ├─ "내 위치 보기" 버튼          브라우저 위치권한으로 내 위치를 파란 점으로 표시
  ├─ 지도 확대                    시군 이름을 안 눌러도, 충분히 확대하면(레벨 7 이하)
  │                               지도 중심에 있는 시군으로 자동 이동
  └─ 시군 클릭
       ↓
MapView (/map/:cityId)           해당 시군의 공공기관 지도
  ├─ 학교 설정 시: 🏫 학교 카테고리 = 지정한 그 학교, 나머지 카테고리는 학교에서 최근접 시설(⭐ 별도 마커 없음)
  ├─ 내 위치(GPS) 설정 시: ⭐ 내 위치 마커 + 학교 포함 전 카테고리 최근접 시설(대체할 실제 학교가 없어 최근접 유지)
  ├─ 미설정 시: 시군 대표 시설
  ├─ 지도 축소                    처음 화면보다 두 단계 이상 축소하면 시군 선택으로 복귀
  ├─ 마커 클릭 → 설명 모달(실제 거리뷰 사진 + 설명) → "확인했어요"
  ├─ ProgressBar 갱신, 마커 색상 변경(초록)
  ├─ 우하단 🏠 버튼(HomeFab)      RegionSelect·MapView·Result 공통, 언제든 처음으로 복귀
  │                               (MapView에서 설명 모달이 열려 있을 땐 숨김)
  └─ 전부 확인 → "결과 보러 가기"
       ↓
Result (/result/:cityId)         교과서 4분류 활동지 형식으로 정리(실제 확인한 장소 이름 표시)
                                  + "다른 지역 탐험하기" (처음으로는 우하단 HomeFab)
```

## 폴더 구조
```
src/
├── data/
│   ├── places.js          # CITIES, CATEGORIES, CATEGORY_GROUPS(교과서 4분류), getPlacesForCity
│   ├── candidates.js      # 시군별·카테고리별 시설 후보 목록 (총 881곳, 카카오 수집)
│   └── cityBoundaries.js  # 시군 경계 좌표 (통계청 2013 경계, southkorea-maps 단순화 버전)
├── lib/
│   ├── kakaoLoader.js     # 카카오맵 SDK 동적 로드 유틸 (services 라이브러리 포함)
│   ├── geo.js             # 경계 판별(point-in-polygon) + 거리 계산(haversine)
│   └── school.js          # '우리 학교' 설정 저장/조회 (localStorage)
├── components/
│   ├── ui/
│   │   ├── BigButton.jsx
│   │   ├── ProgressBar.jsx
│   │   └── HomeFab.jsx        # 우하단 고정 "처음으로" 원형 버튼
│   └── Header.jsx
├── pages/
│   ├── Home.jsx
│   ├── RegionSelect.jsx   # 충남 지도 + 시군 선택 + 내 위치 보기
│   ├── MapView.jsx        # 시군별 공공기관 지도 + 인포윈도우 + 확인
│   └── Result.jsx         # 미션 완료 화면 (교과서 4분류 활동지 형식)
├── App.jsx
└── main.jsx
```

## 데이터 현황
- 카테고리 13종(교과서 38-39쪽 장소 목록 기준): 우체국 ✉️ / 소방서 🚒 / 경찰서 🚓 / 병원 🏥 / 행정복지센터 🏢 / 보건소 💉 / 학교 🏫 / 도서관 📚 / 체육관 🏀 / 기차역 🚉 / 버스터미널 🚌 / 공연장 🎭 / 공원 🌳
- `CATEGORY_GROUPS`(places.js): 위 13종을 아이스크림 교과서 12·13차시(40~42, 83쪽)의 4분류 활동지 기준으로 묶음 — 안전·편리(우체국·소방서·경찰서·행정복지센터·기차역·버스터미널, 83쪽 "지하철역" 예시 근거로 교통시설 포함) / 건강(병원·보건소) / 교육·문화(학교·도서관·공연장) / 놀이·여가(체육관·공원). Result 화면이 이 분류대로 실제 확인한 장소 이름을 보여줌
- **시군별·카테고리별 후보 여러 곳(총 1598곳) 수집 완료** (2026-07-30, 카카오 장소 검색 API. 이름·주소·좌표 검증, 요양병원·사설 도장·무인창구·ATM·미완공(예정) 시설 등 부적절 항목 필터링)
- **체육관·공연장은 대학·병원·사설 시설을 사람이 직접 검수해 제외 완료** (2026-08-02 기준 15개 시군 전체 검토 완료 — 총 50곳 제외·1곳 명칭 수정·1곳 설명 보강). 장소별 `note` 필드(candidates.js)로 카테고리 공통 설명 대신 개별 설명을 넣을 수 있음(예: 천안반다비체육센터). 기준과 사례별 근거는 `DATA_REVIEW.md` 참조 — 보고서에 그대로 활용 가능하도록 정리해둠
- 지도에는 카테고리마다 **한 곳만** 표시: '우리 학교' 설정 시 학교에서 가장 가까운 후보, 미설정 시 검증된 대표 시설(각 후보 목록의 첫 항목)
- **13개 카테고리 전체를 시군당 최대한 폭넓게 보강**(처음엔 카테고리당 6곳만 모았다가, 넓은 시군에서 병천면처럼 학교 근처에 후보가 아예 없어 먼 곳이 선택되는 사각지대가 발견되어 전체 확장). 기차역·버스터미널처럼 원래 개수가 적은 카테고리는 실제로 그 정도만 존재해 큰 변화는 없음
- 시군당 표시 12~13곳. **기차역이 없는 5개 시군**(서산·금산·부여·청양·태안)은 기차역을 뺀 12곳 → "우리 고장엔 기차역이 없구나"도 학습 포인트. 시군별 개수가 달라도 진행률·배지 자동 대응
- 예외 사항:
  - **계룡시**: 경찰서가 없어 계룡지구대로 대체, 병원은 동네 의원(서울베스트내과의원)
  - **청양군·태안군**: 보건소 자리에 보건의료원, 병원은 동네 의원(대정의원/허내과의원)
  - **논산경찰서**는 실제로 강경읍에 위치 (데이터 오류 아님)
  - **계룡시 버스터미널**: 정식 터미널이 없어 계룡금암정류소로 대체
  - **부여 공연장**(국악의전당)·**서천 공원**(장항송림산림욕장)은 읍 중심에서 다소 떨어진 규암면·장항읍에 위치

## 알려진 이슈 / 논의 필요한 점
1. **소방서·경찰서 마커 겹침**: 아산시의 장재119안전센터와 장재파출소가 실제로 약 30m 거리라, 지도에서 두 마커가 거의 겹쳐 보임(실제 지리 특성, 데이터 오류 아님). 확대해서 어느 정도 완화했으나 완전히 분리되진 않음.
2. **카카오 개발자 콘솔 설정**: 로컬 개발(`http://localhost:5173`)은 등록 완료. 배포 시 실제 도메인도 카카오 Web 플랫폼에 추가 등록 필요.
3. **"내 위치 보기" 기능**: 브라우저 위치 권한 기반. 교실 PC/태블릿에서 위치 권한이 거부되거나 부정확할 수 있음(교육용 참고 기능이며, 미션 확인 자체는 여전히 클릭 기반).
4. **거리뷰 사진**: 마커 모달에 카카오 로드뷰(거리뷰)로 실제 장소 사진 표시. 인터넷 연결 필요하고, 로드뷰가 없는 외진 장소는 사진 없이 설명만 표시됨. 로드뷰가 없는 좌표는 확인되지 않았지만, 있을 경우를 대비해 "거리뷰 사진이 없어요" 문구로 안내함.
   - **가까운 촬영 지점으로 자동 보정 시도**(`MapView.jsx`의 `refinePanoId`): 목표 건물 쪽으로 최대 3걸음(약 18m씩) 다가가며 `getNearestPanoId`를 반복 호출. 다만 화면 밖 좌표(-9999px)에 임시로 띄운 로드뷰가 실제 브라우저에서 안정적으로 초기화되지 않는 경우가 있어 항상 적용되진 않음(경찰서 사례에서 확인) — 실패해도 원래 사진으로 조용히 진행되어 화면이 깨지진 않음.
   - **1차 대응은 안내 문구**: 좌표 보정은 "밑바탕"으로만 남겨두고, 건물이 안 보일 때는 "🔄 건물이 잘 안 보이면, 사진 속 화살표를 눌러 둘러보세요." 문구를 모달에 항상 표시해 사용자가 직접 로드뷰를 드래그/화살표로 둘러보도록 안내함(2026-08-01). 좌표만으로는 나무·건물에 가려짐까지 판단할 수 없어, 사람이 눈으로 확인하는 방식이 더 확실하다고 판단.

## 다음 단계 후보
- 배포 도메인 확정 및 카카오 플랫폼 등록
