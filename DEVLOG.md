# 개발일지

PROJECT_STATUS.md가 "지금 상태" 스냅샷이라면, 이 파일은 **왜 그렇게 했는지 + 뭘 시도했다 안 됐는지 + 지금 뭘 기다리고 있는지**를 시간순으로 남겨두는 곳이다. 컨텍스트가 리셋된 채로 다시 시작할 때, 맨 위 "지금 당장 할 일"부터 보면 된다.

---

## 지금 당장 할 일 (다음 세션 시작 시 먼저 확인)

현재 특별히 대기 중인 항목 없음. 다음에 새로운 요청이 없다면 "다음 단계 후보"(PROJECT_STATUS.md)에서 골라 진행할 것.

---

## 주요 결정과 이유 (시간순)

### 2026-08-02 — '우리 학교' ⭐ 마커를 없애고 '학교' 카테고리에 통합 (옵션 B)
- 이전부터 논의만 하고 미뤄뒀던 문제: 학교를 지정하면 ⭐ 우리 학교 마커와, 13개 카테고리 중 하나인 🏫 학교 마커(최근접 다른 학교)가 따로 떠서 헷갈렸음. 후보 목록에 지정한 학교 자체가 없으면 완전히 다른 학교가 학교 카테고리에 뜨는 게 특히 문제였음.
- 논의했던 두 안 중 **(B) 채택**: 별 마커를 없애고, "학교" 카테고리 자체를 사용자가 검색한 그 학교로 고정 — 이유는 마커 상호작용 방식이 하나로 통일되고, 자기 학교 로드뷰 사진을 보는 재미도 생기고, 13개 미션 개수도 그대로 유지되기 때문.
- 구현: `getPlacesForCity`(`places.js`)에서 `cat === 'school'`이고 실제 학교가 지정돼 있으면(GPS '내 위치'는 제외) 후보 목록에서 고르지 않고 학교 자체(`{name, lat, lng, addr}`)를 그대로 씀. 이때 카테고리 공통 설명 대신 `note: '우리가 다니는 학교예요! 로드뷰로 우리 학교 모습을 살펴볼까요?'`를 넣어 개인화. `MapView.jsx`의 ⭐ 우리 학교 마커 렌더링은 삭제.
- **GPS('내 위치') 기준일 땐 예외**: 실제 학교가 아니라 좌표 하나일 뿐이라 "학교"로 대체할 수 없음 — 이 경우엔 예전처럼 ⭐ "내 위치" 마커를 그대로 남겨두고, 🏫 학교 카테고리는 기존처럼 최근접 실제 학교 후보를 보여줌.
- 학교 검색 시 주소(`road_address_name`/`address_name`)를 안 저장하고 버리고 있었는데, 위 note 문구에 실제 주소를 넣기 위해 `school` 객체에 `addr` 필드를 추가(`Home.jsx`의 `handlePick`, `school.js` 형식 주석 갱신).
- Claude Browser로 서산초등학교 검색 → 지도 이동까지 실제로 확인: 별 마커 없이 🏫 마커가 "서산초등학교"로 바로 뜨고, 클릭하면 주소 + 개인화 문구가 정상 표시됨. '내 위치'로 설정했을 때도 ⭐ 마커와 최근접 학교 마커가 둘 다 정상적으로 따로 뜨는 것 확인.

### 2026-08-02 — 체육관·공연장 데이터 검수 완료
- 15개 시군, 총 149곳(체육관 102 + 공연장 47) 후보를 카카오맵 링크와 함께 정리한 검수 표(아티팩트, 2026-08-01 게시)를 바탕으로 교사가 직접 하나씩 확인, 최종 **50곳 제외 · 1곳 명칭 수정(공주국민체육센터) · 1곳 설명 보강(천안반다비체육센터)**. 판단 기준·시군별 상세 내역은 `DATA_REVIEW.md`에 보고서용으로 정리 완료(계룡시는 후보 3곳 모두 공공시설 기준 충족으로 제외 없이 유지).
- 자동 판별의 한계: "국민"이 이름에 있으면 공공시설로 자동 분류되는데, 대학 부속시설이 우연히 "국민체육센터"라는 이름을 쓴 경우(공주국민체육센터)엔 걸러지지 않았음 — 검수 표의 "확인 필요만" 기본 필터에 안 걸리고 숨어있었음. 자동 필터는 우선순위 참고용일 뿐이라는 걸 재확인함.
- 계기: 서산초등학교를 '우리 학교'로 지정했더니 체육관 카테고리에 "충의체육관"이 떴는데, 서산시청 공식 체육시설 목록(서산시민체육관·종합운동장·국민체육센터)엔 없는 곳이라 사설/소속 불명 시설로 확인되어 제외함(WebSearch로 서산시청 공식 목록 직접 대조).
- **주의(재발 방지용 기록)**: 이 버그는 후보 목록 "순서"를 바꿔도 안 고쳐짐 — 학교 지정 시엔 거리 계산으로 최근접 후보를 뽑기 때문에, 목록에서 순서가 아니라 **존재 자체를 지워야** 함. 나중에 다른 카테고리(예: 병원, 도서관)에서 비슷한 문제가 생기면 이 방식을 그대로 적용할 것.

### 2026-07-07 — 기초 확장
- 아산시(4곳)만 있던 데이터를 나머지 14개 시군으로 확장(카카오 장소 검색 API로 우체국/소방서/경찰서/병원 좌표 수집).
- 시군 지도에서 확대하면 자동으로 해당 시군 지도로 진입 / MapView에서 축소하면 시군 선택으로 복귀하는 내비게이션 추가. 카카오맵 줌 애니메이션 도중 페이지를 옮기면 다음 지도의 마커가 사라지는 버그가 있어서, `zoom_changed` 이벤트 후 300ms 뒤에 이동하도록 고침(애니메이션 완료 대기).
- 시군 경계선(통계청 2013 경계 데이터, southkorea-maps 단순화 버전) 추가 — 이후 "내 위치가 어느 시군인지" 판별(`findCityIdAt`)에도 재사용됨.
- 마커 클릭 시 카카오 로드뷰(거리뷰) 실제 사진을 모달에 띄우는 기능 추가. 로드뷰가 모달처럼 나중에 나타나는 컨테이너 안에서 안 그려지는 버그가 있어서, 화면에 완전히 그려진 뒤(rAF 2회) 로드뷰를 생성하고 `relayout()`을 여러 번 호출하도록 처리.

### 2026-07-29~30 — 카테고리 확장 + 개인화
- 카테고리를 4개(우체국/소방서/경찰서/병원) → 9개(행정복지센터/보건소/학교/도서관/체육관 추가) → **13개**(기차역/버스터미널/공연장/공원 추가, 교과서 38-39쪽 장소 목록과 정확히 일치)로 두 차례 확장.
- **'우리 학교' 기능 도입**: 처음엔 아산시 특정 좌표만 하드코딩돼 있었는데, 이러면 "○○초 전용 앱"이 되어 디지털연구대회 심사(범용성)에 불리하다고 판단. 대신 시작 화면에서 학교 이름을 검색하게 하고, 카테고리마다 그 학교에서 가장 가까운 후보를 자동으로 고르는 구조(`getPlacesForCity(cityId, school)`)로 바꿈 — 충남 어느 학교로도 그대로 작동함.
- 이 과정에서 **데이터 사각지대 발견**: 처음엔 카테고리당 후보를 6곳만 모았는데, 천안처럼 넓은 시군에서 병천면 같은 외곽 지역 학교를 지정하면 근처에 후보가 아예 없어 엉뚱하게 먼 곳(예: 동남구소방서)이 선택되는 문제가 있었음. → 소방서/경찰서부터 넓게 재수집 → 결국 **13개 카테고리 전체, 15개 시군 전체를 폭넓게 재수집**(최종 1598곳, `candidates.js`).
- 반응형 크기 조정(`clamp()` 기반)으로 태블릿/데스크톱 화면 크기 대응. 나중에 사용자가 "데스크톱에서 더 커도 될 것 같다"고 해서 데스크톱 쪽 상한값만 별도로 올림(태블릿 쪽 크기는 영향 없게).
- '우리 학교' 옆에 **"내 위치로 바로 찾기"(GPS) 옵션**을 나란히 추가(학교 이름 검색을 대체하지 않고 보조 수단으로). 데스크톱은 GPS 정확도가 떨어질 수 있다는 점을 문구로 안내.
- 우하단 고정 🏠 버튼(HomeFab)을 RegionSelect/MapView/Result에 공통으로 추가 — 지도 탐험 중 처음으로 돌아갈 방법이 마땅치 않던 문제 해결. MapView에서 설명 모달이 열려 있을 땐 겹치지 않게 숨김.
- 한글 문장이 단어 중간에서 줄바꿈되는 문제(`word-break: keep-all` 누락) 발견 및 수정.

### 2026-07-31 — 로드뷰 카메라 보정 시도 (부분 성공)
- 로드뷰가 뜰 때 카메라가 임의의 방향(도로 진행 방향)을 봐서 건물이 화면 밖에 있는 경우가 많았음 → 파노라마 위치에서 목표 건물까지의 방위각을 계산해 카메라를 그쪽으로 돌리는 기능(`bearingDegrees`) 추가.
- 여기서 **NaN 버그 발견**: 로드뷰 `init` 이벤트 직후엔 파노라마 좌표가 아직 `null`일 때가 있어서, 그대로 방위각 계산하면 `NaN`이 나오고 그걸 카메라에 넣으면 **로드뷰 렌더링 자체가 깨짐**. 좌표가 유효할 때만 적용하도록 가드 추가.
- 사용자가 "경찰서 앞에서 화살표 3번 누르면 건물이 잘 보이던데, 자동으로 그렇게 안 되나"라고 물어서, 카카오 로드뷰 SDK를 직접 뜯어봄(`Object.getOwnPropertyNames`) — **"화살표로 이동" 기능은 API에 아예 노출돼 있지 않음**(`setPanoId`/`getPosition`/`setViewpoint`/`relayout`뿐). 대신 목표 쪽으로 조금씩(약 18m씩, 최대 3걸음) 다가가며 `getNearestPanoId`를 반복 호출하는 방식으로 흉내내는 `refinePanoId` 구현.
- 그런데 사용자가 실제 브라우저에서 테스트해보니 **반영이 안 된 것처럼 보임**. 원인으로 추정한 건: 화면 밖 아주 먼 좌표(-9999px)에 띄운 임시 로드뷰가 브라우저에 따라 초기화가 안정적이지 않을 수 있다는 것. 완전히 고치는 대신, **"🔄 건물이 잘 안 보이면, 사진 속 화살표를 눌러 둘러보세요"라는 안내 문구를 1차 해결책으로 추가**하고, 좌표 보정 로직은 "밑바탕"으로만 남겨둠(성공하면 좋고 실패해도 원래 사진으로 조용히 넘어가서 손해는 없음).
- 이 과정에서 자동화 테스트 환경(Claude Browser 샌드박스) 자체가 로드뷰 렌더링에 자주 실패한다는 것도 확인함 — 실제 사용자 Chrome에서는 정상 작동. 로드뷰 관련 기능을 검증할 땐 이 차이를 염두에 둘 것(에러 없이 사진만 안 뜨면 대개 샌드박스 문제).

### 2026-08-01 — 결과 화면을 교과서 활동지 형식으로 재설계
- 사용자가 실제 쓰는 아이스크림 교과서 PDF 스크린샷을 여러 장 보여주며 비교: 교과서는 "안전하고 편리한 생활 / 건강한 생활 / 교육과 문화생활 / 놀이와 여가 생활" **4분류 표**에 장소 **이름**을 적게 돼 있는데, 우리 앱 결과 화면은 그냥 카테고리 아이콘 13개만 나열하고 있어서 활동지 작성에 못 씀.
- 교과서 12차시(40-42쪽) 예시는 기차역·버스터미널을 4분류 어디에도 안 넣었는데, 13-14차시(83쪽) 예시 답안엔 "지하철역"이 "안전하고 편리한 생활" 칸에 들어있는 걸 발견 — 이 근거로 기차역·버스터미널을 안전·편리 그룹에 포함시키기로 결정. `CATEGORY_GROUPS`(`places.js`)로 13개 카테고리를 4분류에 정확히 매핑(남는 카테고리 없음).
- `Result.jsx`를 4분류 카드 + 실제 확인한 장소 이름 칩(chip) 형식으로 전면 재작성. 카테고리 아이콘만 보여주던 `Badge` 컴포넌트는 더 이상 안 쓰여서 삭제.
- 기차역이 없는 시군(서산 등)에서도 해당 칸이 자연스럽게 5개로 줄어들 뿐 안 깨지는 것 확인.

### 2026-08-01 — 체육관·공연장 공공성 문제 발견 (진행 중, 위 "지금 당장 할 일" 참조)
- 서산초등학교 지정 시 "충의체육관"이 뜬 것을 계기로, "체육관"·"공연장" 카테고리가 유독 공공/민간 구분이 이름만으로 어렵다는 걸 확인.
- 처음엔 "후보 목록 순서를 공적 표지(시민/국민/종합 등) 있는 것 우선으로 재정렬"을 제안했으나, **학교를 지정한 경우엔 거리 기반 최근접 선택이라 목록 순서가 전혀 영향을 안 준다는 걸 사용자가 지적** — 순서 재정렬은 이 버그의 해결책이 아님. 실제 해결책은 "공공시설이 아니라고 확인되는 후보를 목록에서 제거"뿐.
- 자동 판별이 어려워서, 사람이 직접 훑어보는 쪽으로 결정. 149곳(체육관 102 + 공연장 47)을 카카오 장소 검색으로 재조회해 장소 ID를 얻고, 카카오맵 상세 페이지 링크·주소·공적 표지 유무·좌표 중복 의심 여부를 표로 정리해 아티팩트로 게시함. 이름에 시민/국민/종합 등 표지가 없는 99곳을 기본 필터로 먼저 보여주도록 설계.

---

## 앞으로 참고할 개발 팁 (이번 세션에서 배운 것)

- **대량 데이터를 브라우저 → 파일로 옮길 때**: 채팅으로 긴 JSON을 옮겨 적으면 좌표 오타 위험이 있음. 로컬에 임시 HTTP 서버(`scratchpad/*.mjs`, Node `http` 모듈)를 띄우고 브라우저에서 `fetch(..., {method:'POST', body: JSON.stringify(data)})`로 보내 파일로 저장하는 방식이 안전하고 빠름. 다 쓰면 서버 프로세스 꼭 종료할 것(포트 충돌 방지).
- **Claude Browser 샌드박스에서 로드뷰 인스턴스를 여러 개 만들면** WebGL 컨텍스트가 소진돼서 `init` 이벤트가 아예 안 뜨는 경우가 있음 — 로드뷰 관련 디버깅이 이상하게 안 될 땐 새 탭(`tabs_create`)에서 다시 시도해볼 것.
- **미리보기 서버(`preview_start`)를 확인 후 끄지 말 것** — 사용자가 실제 브라우저로 같은 `localhost:5173`을 보고 있는 경우가 많아서, 확인 끝났다고 서버를 끄면 사용자 쪽에서 "사이트에 연결할 수 없음" 오류가 남.
- **카카오 로드뷰 API엔 "화살표 이동" 같은 이동 기능이 SDK에 노출돼 있지 않음**(`setPanoId`/`getPosition`/`getViewpoint`/`setViewpoint`/`relayout`/`show`/`hide`가 전부). 비슷한 효과가 필요하면 `getNearestPanoId`를 좌표를 조금씩 옮겨가며 반복 호출하는 방식으로 흉내내야 함.
